	<!-- right answer -->
	<?php if ($this->isAnswerRight): ?>
		<div class="success">Deine Antwort ist richtig. Sehr gut!</div>
		<table class="sorter-numbers">
			<?php foreach ($this->rightAnswer['keys'] as $key): ?>
				<tr><td><?php echo $key; ?></td></tr>
			<?php endforeach; ?>
		</table>
		<table class="sorter-answers">
			<?php foreach ($this->rightAnswer['answers'] as $rightAnswer): ?>
				<tr><td><?php echo $rightAnswer; ?></td></tr>
			<?php endforeach; ?>
		</table>

	<!-- wrong answer -->
	<?php else: ?>	
		<div class="error">Deine Antwort ist leider falsch.</div>
		<table class="sorter-numbers">
			<?php foreach ($this->rightAnswer['keys'] as $key): ?>
				<tr><td><?php echo $key; ?></td></tr>
			<?php endforeach; ?>
		</table>
		<table class="sorter-answers" style="float: left">
			<?php foreach ($this->rightAnswer['answers'] as $rightAnswer): ?>
				<tr><td><?php echo $rightAnswer; ?></td></tr>
			<?php endforeach; ?>	
		</table>
		<table class="sorter-numbers">
			<?php foreach ($this->rightAnswer['keys'] as $key): ?>
				<tr><td><?php echo $key; ?></td></tr>
			<?php endforeach; ?>
		</table>
		<table class="sorter-answers">
			<?php foreach ($this->myAnswer['answers'] as $myAnswer): ?>
				<tr><td><?php echo $myAnswer; ?></td></tr>
			<?php endforeach; ?>	
		</table>
	<?php endif; ?>

